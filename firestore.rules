rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read and write access to all documents for any authenticated user.
    // This is a permissive rule suitable for development.
    match /{document=**} {
      allow get, list, create, update, delete: if request.auth != null;
    }
  }
}
